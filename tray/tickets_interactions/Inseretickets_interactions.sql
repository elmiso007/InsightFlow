INSERT INTO tray.tickets_interactions (
    number,
    interaction_id,
    created_at,
    user_id,
    user_name,
    user_email,
    user_type,
    comments_content,
    comments_type,
    is_public_comment,
    channel_name,
    priority_name,
    group_assigned_name,
    requester_name,
    organization_name,
    tags,
    status,
    department,
    type_name,
    cc,
    form_name,
    data_insercao,
    data_modificacao,
    source         
) 
SELECT 
    SI.number,
    SI.interaction_id,
    SI.created_at,
    SI.user_id,
    SI.user_name,
    SI.user_email,
    SI.user_type,
    SI.comments_content,
    SI.comments_type,
    SI.is_public_comment,
    SI.channel_name,
    SI.priority_name,
    SI.group_assigned_name,
    SI.requester_name,
    SI.organization_name,
    SI.tags,
    SI.status,
    SI.department,
    SI.type_name,
    SI.cc,
    SI.form_name,
    SI.data_insercao,
    SI.data_modificacao,
    SI.source 
 FROM tray.stg_tickets_interactions SI
 WHERE NOT EXISTS (SELECT 1 FROM tray.tickets_interactions AS I WHERE SI.interaction_id = I.interaction_id )